const initialState = {
  validating: false,
  isValidated: false,
  accessToken: '',
  uid: '',
  client: '',
  currentUser: {}
}

const token = (state = initialState, action) => {
  switch(action.type) {
    default:
    return state;
  }
}
